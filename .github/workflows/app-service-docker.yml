#name: App Service Docker
#
#on:
#  push:
#    branches:
#      - prod
#  workflow_dispatch:
#
#env:
#  REGISTRY: ghcr.io
#  IMAGE_NAME: dikaver/web-thebakerz
#  AZURE_WEBAPP_NAME: web-thebakerz
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#
#    permissions:
#      contents: read
#      packages: write
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Log in to GitHub container registry
#        uses: docker/login-action@v3
#        with:
#          registry: ${{ env.REGISTRY }}
#          username: ${{ github.actor }}
#          password: ${{ secrets.GITHUB_TOKEN }}
#
#      - name: Build and push container image to registry
#        uses: docker/build-push-action@v4
#        with:
#          push: true
#          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
#          file: ./Dockerfile
#          build-args: # Pass secrets as build arguments
#            POSTGRES_URL: ${{ secrets.POSTGRES_URL }}
#            POSTGRES_PRISMA_URL: ${{ secrets.POSTGRES_PRISMA_URL }}
#            POSTGRES_URL_NO_SSL: ${{ secrets.POSTGRES_URL_NO_SSL }}
#            POSTGRES_URL_NON_POOLING: ${{ secrets.POSTGRES_URL_NON_POOLING }}
#            POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
#            POSTGRES_HOST: ${{ secrets.POSTGRES_HOST }}
#            POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
#            POSTGRES_DATABASE: ${{ secrets.POSTGRES_DATABASE }}
#            AUTH_TRUST_HOST: ${{ secrets.AUTH_TRUST_HOST }}
#            AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
#            AUTH_GOOGLE_ID: ${{ secrets.AUTH_GOOGLE_ID}}
#            BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}
#            NEXT_PRIVATE_API_SECRET_KEY: ${{ secrets.NEXT_PRIVATE_API_SECRET_KEY }}
#            NEXT_PUBLIC_API_BASE_URL: ${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
#            NEXT_PUBLIC_GOOGLE_API_KEY: ${{ secrets.NEXT_PUBLIC_GOOGLE_API_KEY }}
#            KV_URL: ${{ secrets.KV_URL }}
#            KV_REST_API_URL: ${{ secrets.KV_REST_API_URL }}
#            KV_REST_API_TOKEN: ${{ secrets.KV_REST_API_TOKEN }}
#            KV_REST_API_READ_ONLY_TOKEN: ${{ secrets.KV_REST_API_READ_ONLY_TOKEN}}
#            AUTH_EMAIL_SERVER: ${{ secrets.AUTH_EMAIL_SERVER}}
#            EMAIL_FROM: ${{ secrets.EMAIL_FROM}}
#            AUTH_SENDGRID_SECRET: ${{ secrets.AUTH_SENDGRID_SECRET }}
#
#  deploy:
#    runs-on: ubuntu-latest
#    needs: build
#
#    environment:
#      name: production
#      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
#
#    steps:
#      - name: Deploy to Azure Web App
#        id: deploy-to-webapp
#        uses: azure/webapps-deploy@v3
#        with:
#          app-name: ${{ env.AZURE_WEBAPP_NAME }}
#          publish-profile: ${{ secrets.PUBLISH_PROFILE_DOCKER }}
#          images: "${{ env.REGISTRY }}/${{ github.repository }}:${{ github.sha }}"