/**
 * @fileoverview Intentionally empty placeholder module exporting an empty object.
 *
 * Used by the Turbopack resolveAlias configuration in next.config.ts to stub out
 * Node.js built-in modules (fs, stream, crypto, net, tls, dns, string_decoder)
 * when they are referenced in browser bundles.
 */
export default {};
